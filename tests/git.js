const {expect} = require('chai');
const {gitHistory, gitFileTree, gitFileContent} = require('../utils/git');

describe('git', () => {
    describe('gitHistory', () => {
        it('Должен вернуть объект ответа', async () => {
            //console.log(await gitHistory());

        });
    });
});